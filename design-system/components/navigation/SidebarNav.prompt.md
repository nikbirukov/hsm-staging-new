Primary navigation for the web dashboard.

```jsx
<SidebarNav active="dash" onSelect={setTab} items={[{id:'dash',label:'Dashboard',icon:'space_dashboard'},{id:'act',label:'Actions',icon:'flag',count:12}]} />
```
