The canonical Pass / Fail / N/A vocabulary. Never invent new outcome colours — green passes, red fails, grey is N/A, amber is an open action.

```jsx
<StatusPill status="fail" />
<StatusPill status="overdue" size="sm" />
```
