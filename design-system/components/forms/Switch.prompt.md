Immediate-effect toggle (settings, notification preferences). Use Checkbox inside forms that are saved with a button.

```jsx
<Switch checked={sync} onChange={setSync} label="Auto-sync when back online" />
```
