The core inspection interaction. Answer buttons are 44px minimum for gloved use on a warehouse floor.

```jsx
<ChecklistRow index={4} question="Are all racking beams free of visible damage?" value={a} onChange={setA} photos={2} />
```
