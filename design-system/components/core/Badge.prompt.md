Pill label. For inspection outcomes use `StatusPill` instead — Badge is for everything else (counts, plan tiers, "New").

```jsx
<Badge tone="brand">Pro</Badge>
<Badge tone="warn" icon="schedule">Due in 3 days</Badge>
```
