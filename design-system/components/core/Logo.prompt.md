The hsmanager brand lockup. Use `tone="white"` on navy or photo backgrounds; set `basePath` to reach `assets/` from your page.

```jsx
<Logo height={40} basePath="../../assets" />
<Logo variant="mark" height={28} basePath="../../assets" />
```
